{
  "name": "fineko-flows-microservices",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "image-processor",
    "slide-builder",
    "video-processor",
    "remotion-renderer"
  ],
  "scripts": {
    "dev": "npm run dev -w image-processor & npm run dev -w slide-builder & npm run dev -w video-processor & npm run dev -w remotion-renderer",
    "start": "npm run start -w image-processor & npm run start -w slide-builder & npm run start -w video-processor & npm run start -w remotion-renderer"
  },
  "engines": {
    "node": ">=20"
  }
}
